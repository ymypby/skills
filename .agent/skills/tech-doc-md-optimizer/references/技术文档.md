<!-- COMMENT: Template structure for Chinese technical markdown documents.

=== STRUCTURE ===
**Order**: 目录 → 版本说明 → 组件概述 → 核心概念 → 使用说明 → 参考

**TOC Generation**:
- 3 levels: ## (no indent), ### (2-space), #### (4-space)
- Format: `- [标题](#标题)`
- Example:
  ```
  - [目录](#目录)
  - [版本说明](#版本说明)
  - [使用说明](#使用说明)
    - [一、前置条件](#一前置条件)
      - [1、依赖引入](#1 依赖引入)
    - [二、功能特性](#二功能特性)
      - [1、日志脱敏](#1 日志脱敏)
  ```

**Formatting**:
- Config/code → fenced blocks with language hints
- Tables → markdown table syntax
- Diagrams → mermaid (when: source has diagram OR complex flow: ≥3 steps, ≥2 parties, branches, loops, state changes)

**Boundaries**:
- Global configs → `### 一、前置条件`
- Feature configs → `### 二、功能特性`
- Multiple approaches → "方式一/方式二"
- No source info → "待补充"

**Source Code**: When provided, trust code over docs, discover missing features.

=== IMPORTANT ===
Template examples are illustrative only - replace with real content.
-->

## 目录

<!-- COMMENT: Auto-generated TOC with 3-level depth. Generate after document complete. -->

- [目录](#目录)
- [版本说明](#版本说明)
- [组件概述](#组件概述)
- [核心概念](#核心概念)
- [使用说明](#使用说明)
  - [一、前置条件](#一前置条件)
    - [1、依赖引入](#1 依赖引入)
    - [2、数据库](#2 数据库)
    - [3、其他前置条件](#3 其他前置条件)
  - [二、功能特性](#二功能特性)
    - [1、日志脱敏](#1 日志脱敏)
- [参考](#参考)

## 版本说明

<!-- COMMENT: Table with columns: 发布日期 | 修改人 | 版本号 | 修改内容. No history → keep header + "待补充" row. -->

| 发布日期   | 修改人   | 版本号 | 修改内容               |
| ---------- | -------- | ------ | ---------------------- |
| 2026-02-28 | 张三     | 0.1.0  | 初始版本，待进一步完善 |

## 组件概述

<!-- COMMENT: High-level overview (what & why). 1-3 short paragraphs or bullets. Avoid implementation details.
DISTINCT FROM "功能特性": This = business value; 功能特性 = how-to steps. -->

本节简要说明系统的核心能力和主要使用场景，例如：

- 统一为各业务系统提供通知发送能力（短信、邮件等）
- 支持模板化配置和多渠道扩展
- 提供统一的监控和失败重试机制

## 核心概念

<!-- COMMENT: Only domain-specific concepts (internal modules, states, policies). NOT generic concepts (HTTP, DB, cache) unless special meaning here. No new concepts → "暂无新增核心概念". -->

本节用于汇总并解释仅在本组件/系统中出现、且对理解设计和使用方式非常重要的核心概念，例如：

- "通知任务（NotificationJob）"：表示一次从业务侧发起到最终渠道发送完成的完整通知任务生命周期
- "渠道适配器（ChannelAdapter）"：屏蔽不同短信/邮件服务商差异的适配层，实现统一调用接口

## 使用说明

<!-- COMMENT: Step-by-step usage guide. Structure: 前置条件 (global) + 功能特性 (per-feature). Optional mermaid diagram after heading for overall flow. -->

以"统一通知发送组件"为例，本节说明从引入依赖、准备数据库到具体功能使用的完整使用流程，开发者可按需替换为自身系统的实际配置。

### 一、前置条件

<!-- COMMENT: Global prerequisites shared by ALL features. NOT per-feature details. Configs here should NOT repeat in 功能特性. -->

#### 1、依赖引入

<!-- COMMENT: Maven/Gradle coordinates. -->

在项目构建工具中引入本组件的依赖（例如 Maven 坐标），确保在编译与运行时均可正常加载。例如，在 Maven 中添加：

```xml
<dependency>
  <groupId>com.company.notification</groupId>
  <artifactId>notification-starter</artifactId>
  <version>1.0.0</version>
</dependency>
```

#### 2、数据库

<!-- COMMENT: Required tables, indexes, initialization data. -->

如功能依赖数据库表结构，需要提前在目标环境中创建或更新相关表结构，并完成基础数据初始化。以通知发送记录表为例：

```sql
CREATE TABLE notify_message (
  id           BIGINT       PRIMARY KEY,
  biz_id       VARCHAR(64)  NOT NULL COMMENT '业务单号',
  receiver     VARCHAR(64)  NOT NULL COMMENT '接收人标识（如手机号、邮箱）',
  channel      VARCHAR(32)  NOT NULL COMMENT '发送渠道（SMS/EMAIL 等）',
  template_code VARCHAR(64) NOT NULL COMMENT '模板编码',
  status       VARCHAR(16)  NOT NULL COMMENT '发送状态',
  created_at   TIMESTAMP    NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

#### 3、其他前置条件

<!-- COMMENT: Other system-level prerequisites: config center keys, middleware, framework versions. -->

例如，可以根据实际情况补充以下前置条件：

- 配置中心中已为通知组件创建基础配置（如发送限流开关、渠道开关等）
- 已部署并可访问的 Redis 或消息队列，用于缓冲发送任务或记录重试信息
- 应用统一使用指定版本及以上的日志框架（如 Logback 1.2+），以支持日志脱敏过滤器

### 二、功能特性

<!-- COMMENT: Per-feature complete usage flows. One item = one independent feature. Include: config → code integration → API call → verification. Do NOT repeat 前置条件 configs. -->

本章节按功能点说明完整的使用流程，帮助开发者在实际业务中快速落地。

#### 1、日志脱敏

<!-- COMMENT: Example feature. Replace with actual features. One feature per item. -->

以"通知发送组件"在记录用户手机号等敏感信息时的日志脱敏为例，完整使用流程如下：

**步骤一：配置脱敏规则**

根据项目实际情况，可选择以下任一方式配置脱敏规则：

**方式一：YAML 配置文件方式**

在 `application.yml` 中声明需要脱敏的字段及规则：

```yaml
logging:
  mask:
    enabled: true
    fields:
      - name: phoneNo
        pattern: "^(\\d{3})\\d{4}(\\d{4})$"
        replacement: "$1****$2"
      - name: idCardNo
        pattern: "^(\\d{4})\\d+(\\d{4})$"
        replacement: "$1********$2"
```

**方式二：Properties 配置文件方式**

在 `application.properties` 中声明需要脱敏的字段及规则：

```properties
logging.mask.enabled=true
logging.mask.fields[0].name=phoneNo
logging.mask.fields[0].pattern=^(\\d{3})\\d{4}(\\d{4})$
logging.mask.fields[0].replacement=$1****$2
logging.mask.fields[1].name=idCardNo
logging.mask.fields[1].pattern=^(\\d{4})\\d+(\\d{4})$
logging.mask.fields[1].replacement=$1********$2
```

**方式三：代码配置方式**

通过 Java 配置类自定义脱敏规则：

```java
@Configuration
public class LogMaskConfig {
    @Bean
    public LogMaskProperties logMaskProperties() {
        LogMaskProperties properties = new LogMaskProperties();
        properties.setEnabled(true);
        properties.addField("phoneNo", "^(\\d{3})\\d{4}(\\d{4})$", "$1****$2");
        properties.addField("idCardNo", "^(\\d{4})\\d+(\\d{4})$", "$1********$2");
        return properties;
    }
}
```

**步骤二：引入脱敏组件**

在 Spring Boot 启动类或配置类上添加注解启用脱敏功能：

```java
@SpringBootApplication
@EnableLogMasking  // 启用日志脱敏
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
```

**步骤三：在业务代码中使用**

脱敏组件会自动拦截并处理日志输出，业务代码无需额外修改：

```java
@Slf4j
@Service
public class NotificationService {
    public void sendSms(String phoneNo, String content) {
        // 日志中的 phoneNo 会自动脱敏
        log.info("发送短信至手机号：{}, 内容：{}", phoneNo, content);
        // ... 发送逻辑
    }
}
```

**步骤四：验证脱敏效果**

启动应用后查看日志输出，确认敏感字段已被正确脱敏：

```
2026-02-28 10:00:00 INFO  - 发送短信至手机号：138****1234, 内容：您的验证码为 123456
```

## 参考

<!-- COMMENT: Additional materials: diagrams, wiki links, API docs, external references. Ask user for missing reference materials. -->

本章节为读者提供进一步理解系统的参考资料，推荐包含但不限于以下内容（可根据项目情况增删）：

- 类图与组件关系图：例如 `docs/notification-component-class-diagram.png`，展示通知发送组件与业务系统、渠道适配器之间的依赖关系。
- 关键时序图：例如 `docs/notification-send-sequence.puml`，说明业务系统发起发送请求到最终渠道发送成功/失败的完整调用链。
- 内部 Wiki 文档链接：如 `[通知组件设计说明](https://wiki.example.com/notification/design)`、`[通知模板配置规范](https://wiki.example.com/notification/template)`。
- 外部参考文档：如 MyBatis、Spring Boot Logging、日志脱敏最佳实践等官方文档链接，用于深入了解相关技术栈。